---
title: "Fix the bug"
format:
  html:
    toc: false
filters:
  - webr
---
```{webr-r}
webr::install("ggplot2")
library(ggplot2)
```


```{webr-r}
webr::install("ggplot2")
library(ggplot2)

url <- "https://dalcph.github.io/hw-3/data/children.csv"
download.file(url, "children.csv")
namedf <- read.csv("children.csv")
```

```{webr-r}
fit = lm(mpg ~ am, data = mtcars)

summary(fit)
```

```{webr-r}
fit = lm(mpg ~ am, data = mtcars)

summary(fit)
```
